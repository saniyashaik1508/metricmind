name: 'metricmind'
version: '1.0.0'
config-version: 2

profile: 'metricmind'

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  metricmind:
    staging:
      +materialized: view
    marts:
      +materialized: table
